<?xml version="1.0" encoding="UTF-8"?>
<b:beans xmlns="http://www.springframework.org/schema/security"
         xmlns:b="http://www.springframework.org/schema/beans"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
						 http://www.springframework.org/schema/security
						 http://www.springframework.org/schema/security/spring-security-4.1.xsd">


    <http auto-config="true" use-expressions="true">
        <csrf disabled="true"/>
        <intercept-url pattern="/" access="isAuthenticated()" />
        <intercept-url pattern="/login" access="permitAll"/>
        <intercept-url pattern="/users/**" access="hasAuthority('1')"/>
        <intercept-url pattern="/groups/**" access="hasAuthority('1')"/>
        <intercept-url pattern="/log/**" access="isAuthenticated()"/>
        <intercept-url pattern="/questions/**" access="isAuthenticated()"/>
        <intercept-url pattern="/chapters/**" access="isAuthenticated()"/>
        <intercept-url pattern="/test/**" access="isAuthenticated()"/>
        <intercept-url pattern="/testRun/**" access="isAuthenticated()"/>
        <form-login login-page="/login" authentication-failure-url="/login?error=1" default-target-url="/"/>
        <logout logout-url="/logout" logout-success-url="/login?logout=1" invalidate-session="true"/>
        <remember-me
            key="testAppKEY"
            token-validity-seconds="259200"/>
    </http>

    <authentication-manager>
        <authentication-provider>
            <jdbc-user-service data-source-ref="dataSource"
                               users-by-username-query="SELECT username, password, true FROM USER WHERE username=?"
                               authorities-by-username-query="SELECT username, type FROM USER, USER_GROUP ug WHERE username=? AND GROUP_ID=ug.ID"/>

            <password-encoder hash="sha-256"/>
        </authentication-provider>
    </authentication-manager>

</b:beans>
