<?xml version="1.0" encoding="UTF-8"?>
<b:beans xmlns="http://www.springframework.org/schema/security"
         xmlns:b="http://www.springframework.org/schema/beans"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
						 http://www.springframework.org/schema/security
						 http://www.springframework.org/schema/security/spring-security-4.1.xsd">


    <http auto-config="true" use-expressions="true">
        <intercept-url pattern="/" access="isAuthenticated()" />
        <intercept-url pattern="/login" access="permitAll" />
        <!--<intercept-url pattern="/users/**" access="hasRole('TEACHER')" />-->
        <intercept-url pattern="/users/**" access="permitAll" />
        <form-login login-page="/login" authentication-failure-url="/login?error=1"/>
        <logout logout-url="/logout" logout-success-url="/login?logout=1"/>
        <remember-me
            key="testAppKEY"
            token-validity-seconds="259200"/>
    </http>

    <authentication-manager>
        <authentication-provider>
            <!--<user-service>
                <user name="user" password="password" authorities="ROLE_ADMIN"/>
            </user-service>-->
            <jdbc-user-service data-source-ref="dataSource"
                               users-by-username-query="SELECT username, password, true FROM USER WHERE username=?"
                               authorities-by-username-query="SELECT username, 'TEACHER' FROM USER WHERE username=?"/>


        </authentication-provider>
    </authentication-manager>

</b:beans>
